{
  "name": "test-code-plugilo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "make dev",
    "dev:fe": "pnpm --filter fe dev",
    "dev:be": "pnpm --filter be dev",
    "build": "pnpm -r build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}

